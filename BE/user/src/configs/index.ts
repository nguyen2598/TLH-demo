import app from './app';
// import database from './database'
import hashing from './hashing';
import jwt from './jwt';

const config = {
    app,
    // database,
    jwt,
    hashing,
};

export { config };
